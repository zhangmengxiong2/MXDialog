<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mxRootLay"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/mx_dialog_color_border"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="@dimen/mx_dialog_card_corner_radius">

    <LinearLayout
        android:id="@+id/mxCardLay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/mx_dialog_bg_select_cancel"
        android:orientation="vertical">

        <TextView
            android:id="@+id/titleTxv"
            android:layout_width="match_parent"
            android:layout_height="@dimen/mx_list_item_height"
            android:ellipsize="end"
            android:gravity="center"
            android:singleLine="true"
            android:text="请选择"
            android:textColor="@color/mx_color_text"
            android:textSize="@dimen/mx_size_title"
            android:visibility="gone" />

        <com.mx.dialog.views.MXRatioFrameLayout
            android:id="@+id/contentLay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ListView
                android:id="@+id/listView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#00000000"
                android:divider="@color/mx_dialog_color_divider"
                android:dividerHeight="1dp"
                android:listSelector="#00000000"
                android:scrollbars="none"
                tools:listitem="@layout/mx_dialog_list_item" />
        </com.mx.dialog.views.MXRatioFrameLayout>
    </LinearLayout>

    <TextView
        android:id="@+id/cancelBtn"
        android:layout_width="match_parent"
        android:layout_height="@dimen/mx_list_item_height"
        android:layout_marginTop="@dimen/mx_size_divider_space"
        android:background="@drawable/mx_dialog_bg_select_cancel"
        android:gravity="center"
        android:text="取消"
        android:textColor="@color/mx_color_text"
        android:textSize="@dimen/mx_size_title" />
</LinearLayout>